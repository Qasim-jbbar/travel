import styles from "@/app/loadingg/page.module.css"

const Page = () => {
    return (
        <div className={styles.body}>
            <span className={styles.main}>Loading</span>
        </div>
    );
};

export default Page;
