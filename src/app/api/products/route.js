// import { PrismaClient } from "@prisma/client";
import { NextResponse } from "next/server";

// const prisma = new PrismaClient();

export async function GET(req, res) {
    // let company = company.findMany();
    return NextResponse.json({error:'قبل'})
}

